
// This file is no longer used for backend communication as the app is now Client-Side/Serverless (using LocalStorage).
// We keep the file to avoid breaking imports if any remain, but it exports an empty string.

export const API_BASE_URL = '';
